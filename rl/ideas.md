Use AI image generation to create your own ball skins/textures (like that JSON config you had) based on a prompt
"Generate a neon cyberpunk ball skin" → auto-drops it into your BakkesMod config

2. Option to overwrite AlphaConsole's Folder

3. Make sure it adds and not overwriting the _Presets.json